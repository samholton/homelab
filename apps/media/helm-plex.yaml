apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: plex
  namespace: media
spec:
  interval: 1h
  chart:
    spec:
      chart: app-template
      version: 4.6.2
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 1h
  values:
    controllers:
      plex:
        defaultContainerOptions:
          resources:
              requests:
                  gpu.intel.com/i915: "1"
              limits:
                  gpu.intel.com/i915: "1"
        containers:
          plex:
            image:
              repository: plexinc/pms-docker
              tag: 1.43.0.10492-121068a07
            env:
              TZ: "America/New_York"
              ADVERTISE_IP: "https://plex.samholton.com:443"
    service:
      plex:
        controller: plex
        ports:
          pms:
            port: 32400
            protocol: TCP
    ingress:
      plex:
        className: nginx-external
        annotations:
          cert-manager.io/cluster-issuer: "letsencrypt-prod"
        hosts:
          - host: &host plex.samholton.com
            paths:
              - path: /
                service:
                  identifier: plex
                  port: pms
        tls:
          - hosts:
              - *host
            secretName: plex-tls
    persistence:
      media:
        existingClaim: media-library
      config:
        existingClaim: plex-config
      sam-family-videos:
        type: nfs
        server: truenas
        path: "/mnt/the-pool/k8s/vols/pvc-79096f63-a246-465e-96e2-a6e476f33d4e/data/sam/files/Sam Family Videos"
        advancedMounts:
          plex:
            plex:
              - path: /sam-family-videos
                readOnly: true
